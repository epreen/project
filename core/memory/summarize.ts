// core/memory/summarize.ts
import { think } from "@/oc/rose/think"

export async function summarizeMemories(memories: string[]) {
  // return think({
  //   userInput:
  //     "Summarize the following memories into concise, durable insights.",
  //   memoryContext: memories.join("\n"),
  // })
}